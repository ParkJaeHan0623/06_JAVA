public class Practice03_MyPrint {
    public static void main(String[] args) {
        int num = 10;
        System.out.println(num >= 0 ? (num != 0 ? "양수":0) : "음수");

        num = -9;
        System.out.println(num >= 0 ? (num != 0 ? "양수":0) : "음수");
    }
}
